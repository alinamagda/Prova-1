// Esempio di ciclo for of con un array per sommare i numeri
const numeri = [1, -2, 3, 4, 5];
let somma = 0;
for (const numero of numeri) {
  somma += numero;
}
console.log("La somma di numeri è:", somma);
